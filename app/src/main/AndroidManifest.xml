<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dandi.faq">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name="com.example.faq.sharepreference.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name=".ProfileActivity"
            android:parentActivityName=".MainActivity"
            android:label="Profile" />
        <activity android:name=".GrapicActivity" />
        <activity android:name=".MainAdminActivity" />
        <activity android:name=".SettingsUser" />
        <activity android:name=".LoginAdminActivity" />
        <activity android:name=".AddFAQActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/NoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/NoBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".VerifikasiNoTelponActivity" />
    </application>

</manifest>